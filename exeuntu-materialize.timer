[Unit]
Description=Run exeuntu-materialize periodically

[Timer]
# Backstop only: the boot run itself waits for reflection (ExecStartPre), so
# this just retries the rare longer outage and re-checks daily afterwards.
OnBootSec=45s
OnUnitActiveSec=1h
Persistent=true

[Install]
WantedBy=timers.target
